package com.crow.locrowai.api;

import java.util.HashMap;
import java.util.Map;

public class Mapping {
    public Map<String, String> returns_to_params = new HashMap<>();
    public Map<String, String> returns_to_passes = new HashMap<>();
    public Map<String, String> passes_to_params = new HashMap<>();
    public Map<String, String> passes = new HashMap<>();
}
